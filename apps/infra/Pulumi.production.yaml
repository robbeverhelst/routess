config:
  maps:replicas: 2
  maps:namespace: maps
  maps:ingressClassName: cloudflare-tunnel
  maps:hosts:
    - maps.robbeverhelst.com
  # Dynamic configuration set by CI/CD pipeline:
  # - imageTag (from semantic release)
  # - ghcrToken (GitHub token)
  # - dbHost (from 1Password)
  # - dbUser (from 1Password)
  # - dbPassword (from 1Password, secret)
  # - dbName (set to "maps")
  # - jwtSecret (from 1Password, secret)
  # - googleClientId (from 1Password)
