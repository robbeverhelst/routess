// Display colors for the two NodeNetwork kinds (ADR 0033). Shared so the map
// overlay and the layer picker legend never drift apart.
export const NODE_NETWORK_COLORS = {
	hiking: "#dc2626",
	cycling: "#1d4ed8",
} as const;
