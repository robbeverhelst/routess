import { STATIC_PREVIEW_COLORS } from "@routess/core";

// Route preview purple, shared with static map previews so live layers and
// generated preview images stay on the same color.
export const ROUTE_PREVIEW_COLOR = `#${STATIC_PREVIEW_COLORS.route}`;
