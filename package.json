{
	"name": "routess-monorepo",
	"private": true,
	"version": "1.96.3",
	"type": "module",
	"packageManager": "bun@1.1.38",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev": "docker-compose up -d postgres && (docker-compose up -d pgadmin || true) && bun run --filter './packages/*' build && bun run --filter './apps/*' dev",
		"build": "bun run --filter './packages/*' build && bun run --filter './apps/*' build",
		"lint": "biome check .",
		"check-types": "bun run --filter '*' check-types",
		"test": "bun run --filter '*' test",
		"format": "biome format --write .",
		"format:check": "biome format .",
		"ci": "(docker-compose up -d postgres || docker start routess-postgres || true) && bun run format && bun run lint && bun run build && bun run check-types && bun run test",
		"clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules apps/*/dist packages/*/dist coverage apps/*/coverage /tmp/esbuild* && bun install",
		"docker:build:web": "docker build -f apps/web/Dockerfile -t routess-web .",
		"docker:build:api": "docker build -f apps/api/Dockerfile -t routess-api .",
		"ls": "node -e \"const pkg = require('./package.json'); console.log('\\n📋 Available Scripts:\\n'); Object.entries(pkg.scripts).forEach(([name, cmd]) => console.log('  ' + name.padEnd(25) + ' ' + cmd)); console.log('\\n💡 Usage: bun run <script-name>\\n');\""
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.8",
		"@semantic-release/commit-analyzer": "^13.0.0",
		"@semantic-release/git": "^10.0.1",
		"@semantic-release/github": "^11.0.0",
		"@semantic-release/npm": "^12.0.1",
		"@semantic-release/release-notes-generator": "^14.0.1",
		"@types/node": "^24.0.1",
		"semantic-release": "^24.2.4",
		"typescript": "~5.9.0"
	},
	"overrides": {
		"brace-expansion": "^2.0.1"
	}
}
